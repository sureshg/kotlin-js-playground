rootProject.name = "kotlin-js-playground"

dependencyResolutionManagement {
    repositories {
        mavenCentral()
        google()
        jcenter() // For RSocket
    }
}

pluginManagement {
    repositories {
        gradlePluginPortal()
        mavenCentral()
        google()
    }
}
